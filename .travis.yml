language: node_js

install:
  - npm install -g truffle
  - npm install -g ganache-cli
  - npm install
    
matrix: 
 include: 
   - node_js: 8
     env: FOO=foo
   - node_js: 10.15.1
     env: BAR=bar
     env: FOO=foo

script:
  - npm run lint
#     - truffle migrate
#     - truffle test
#     - npm run stop

cache:
  directories:
    - node_modules

after_script:
  - npm run coverage && cat coverage/lcov.info | coveralls
